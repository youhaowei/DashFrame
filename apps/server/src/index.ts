#!/usr/bin/env bun
/**
 * `dashframe serve` — Bun CLI entry for the headless DashFrame server.
 *
 * Stub implementation. The full v0.2 server (DuckDB workspace, WyStack
 * transport, PGLite metadata) will grow from here.
 */

console.log("dashframe serve: not yet implemented, coming v0.2");
process.exit(0);
