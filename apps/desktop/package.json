{
  "name": "@dashframe/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "electron .",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "check": "bun run typecheck && bun run lint"
  },
  "dependencies": {
    "electron": "^33.2.1"
  },
  "devDependencies": {
    "@dashframe/eslint-config": "workspace:*",
    "@types/node": "^22.10.1",
    "typescript": "5.7.2"
  }
}
