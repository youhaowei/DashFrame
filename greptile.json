{
  "files": [
    {
      "path": "PRODUCT.md",
      "description": "What DashFrame is, its commitments (local-first, per-field sensitivity classification \u2014 no shipped import review or at-rest gate yet \u2014 immediate, truthful) and non-goals. Load before judging whether a feature or element belongs."
    },
    {
      "path": "DESIGN.md",
      "description": "Product design: direction and references, surface-system tokens, workbench panes, form/pane conventions, artifact page model, rejected directions, do's and don'ts. Source of truth for any UI review."
    },
    {
      "path": "GLOSSARY.md",
      "description": "Canonical product vocabulary and model-type mapping. Load when reviewing user-facing names or model naming."
    },
    {
      "path": "CLAUDE.md",
      "description": "Project entry point; imports AGENTS.md, which carries the operational rules and the product/design doc map."
    }
  ],
  "customContext": {
    "other": [
      {
        "content": "PRIVACY FLOOR (current behavior, not an at-rest guarantee). Field.sensitivity is a per-field classification carried through the schema: validated on local ingest (apps/server/src/host/local-ingest.ts) and remote import, where the review gate applies the user-approved classification to imported fields (apps/server/src/host/connectors.ts), and surfaced in the UI and in assistant/MCP field descriptors. It does not gate persistence today: row data at rest is per-project and unencrypted, and apps/server/src/host/data-fetch/materializer.ts persists source and result Arrow through DataFrameStorage without a sensitivity check. A stricter data-at-rest floor is a future consideration. What IS enforced: never persist raw sample values in analysis JSON. packages/convex-backend/convex/host.ts strips sampleValues before imported-frame metadata and operation records are persisted (covered by the strips-samples case in packages/convex-backend/test/drafts.test.ts); flag any path that persists sample values or raw rows outside DataFrameStorage.",
        "scope": [
          "packages/engine-server/**",
          "packages/engine-browser/**",
          "packages/server-core/**",
          "apps/server/**",
          "packages/convex-backend/**"
        ]
      },
      {
        "content": "DESTRUCTIVE-RECOVERY rule (invariant). Any path that quarantines, deletes, or overwrites a user's local project or data directory must require POSITIVE evidence of the specific failure it recovers from \u2014 never infer it from a broad error signal. Fail toward preserving data: when corruption cannot be positively confirmed, surface the error instead of destroying user data.",
        "scope": [
          "packages/server-core/**",
          "apps/server/**",
          "apps/desktop/**"
        ]
      },
      {
        "content": "NO PER-SURFACE UI FORKS (invariant). The web and Electron renderers are IDENTICAL. No `isElectron` branches in components; capability differences ride through providers/context (the providerWrapper / engine-injection pattern), never component-level platform checks. Flag any `isElectron`/`window.electron` conditional inside a component's render.",
        "scope": ["packages/app/**", "apps/renderer/**", "apps/web/**"]
      },
      {
        "content": "NO OFF-TOKEN COLOR (invariant). No raw hex/rgb/oklch in classNames or styles. Use @wystack/ui semantic tokens only (neutral-*, palette-*, surface-*). Any off-token color is a finding.",
        "scope": ["packages/app/**", "packages/ui/**", "apps/renderer/**"]
      },
      {
        "content": "NO RAW RUNTIME ERRORS IN USER-FACING UI (invariant). Never surface raw stack traces or engine error strings in dialogs, toasts, or banners. Translate to a human sentence plus a recovery action.",
        "scope": ["packages/app/**", "apps/desktop/**", "apps/renderer/**"]
      },
      {
        "content": "NO TICKET IDS IN SOURCE (CI-enforced). Source code and comments must never contain YW-/TASK- identifiers. Reference the public GitHub issue number (#nn) instead. This is checked by scripts/check-no-ticket-refs.mjs.",
        "scope": ["packages/**", "apps/**"]
      }
    ]
  }
}
