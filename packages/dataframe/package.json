{
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^4.0.12"
  },
  "main": "src/index.ts",
  "name": "@dashframe/dataframe",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist *.tsbuildinfo",
    "dev": "tsc -w -p tsconfig.json --preserveWatchOutput",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "lint": "eslint src",
    "test": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "types": "src/index.ts",
  "version": "0.1.0",
  "dependencies": {
    "@duckdb/duckdb-wasm": "^1.30.0",
    "apache-arrow": "^21.1.0",
    "idb-keyval": "^6.2.2"
  }
}
