/**
 * @dashframe/connector-local
 *
 * Unified local file connector for DashFrame.
 * Handles CSV and JSON file uploads with automatic format detection.
 */

export { LocalFileConnector, localFileConnector } from "./connector";
