{
  "$schema": "https://turbo.build/schema.json",
  "ui": "stream",
  "concurrency": "25",
  "globalPassThroughEnv": ["DASHFRAME_PROJECT_DIR"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**"]
    },
    "dev": {
      // Packages consume each other's built `dist`, so a fresh checkout has
      // nothing to import until its dependencies are built once.
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    },
    "lint": {},
    "test": {
      "dependsOn": ["^build"],
      "cache": true
    },
    "test:coverage": {
      "dependsOn": ["^build"],
      "cache": true,
      "outputs": ["coverage/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "check": {
      "dependsOn": ["lint", "typecheck", "test"],
      "outputs": []
    },
    "clean": {
      "cache": false
    }
  }
}
